const { average, sum } = require("./functions.js")

test('sum of 1 and 2 should be 3', () => {
  expect(sum(1, 2)).toBe(3)
})

test('sum of 1.5 and 2.5 should be 4', () => {
  expect(sum(1.5, 2.5)).toBe()
})

test('<name>', () => {

})